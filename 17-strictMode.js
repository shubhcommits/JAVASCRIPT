"use strict";
let hasDrivingLicence=false;
let passTest = true;
if(passTest) hasDrivinLicence=true;  // it will show error
if(hasDrivingLicence) console.log("Can drive");
const private=357;  // this will show error in console as it will may use further
// .. this show becacuse here we are using strict mode