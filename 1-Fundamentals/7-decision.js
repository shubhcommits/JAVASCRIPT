// if else in js 
const age=15;
if(age>=18){
    console.log(`Eligible for driving`);
}
else{
    console.log(`${18-age} years are  left for driving `);
}

// Another example
const birthYear=2004;
let centuary;
if(birthYear<2000){
    centuary=20;
}
else{
    centuary=21;
}
console.log(`${centuary}`);