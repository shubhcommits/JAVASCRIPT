var name1;
var name2;
var name1per;
var name2per;
name1=prompt("Enter the name of the first person");
name2=prompt("Enter the name of the second person");
name1per=Math.floor((Math.random()*100)+1);
name2per=Math.floor((Math.random()*100)+1);
alert(name1+" loves "+name2+" "+name1per+"%");
alert(name2+" loves "+name1+" "+name2per+"%");
